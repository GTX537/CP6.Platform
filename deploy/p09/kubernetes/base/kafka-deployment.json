{
  "apiVersion": "apps/v1",
  "kind": "Deployment",
  "metadata": {
    "name": "kafka",
    "namespace": "cp6-p09-ci",
    "labels": { "cp6.io/nondeployable": "true" }
  },
  "spec": {
    "replicas": 0,
    "selector": { "matchLabels": { "cp6.io/workload": "kafka" } },
    "template": {
      "metadata": {
        "labels": {
          "cp6.io/nondeployable": "true",
          "cp6.io/workload": "kafka",
          "cp6.io/component": "infrastructure"
        }
      },
      "spec": {
        "serviceAccountName": "provisioner",
        "automountServiceAccountToken": false,
        "containers": [
          {
            "name": "kafka-fixture",
            "image": "example.invalid/cp6/p09-fixture@sha256:aaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaa",
            "imagePullPolicy": "IfNotPresent",
            "args": ["kafka-stub"],
            "ports": [{ "name": "kafka", "containerPort": 9092, "protocol": "TCP" }]
          }
        ]
      }
    }
  }
}
